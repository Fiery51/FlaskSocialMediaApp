from app import app  # Importing the Flask app from the app package

# Main entry point of the application
if __name__ == '__main__':
    app.run(debug=True)  # Starts the Flask app with debug mode enabled for easier development